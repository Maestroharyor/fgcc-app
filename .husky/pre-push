#!/usr/bin/env sh
# SkillUp 1.0 — pre-push gate
# Belt-and-braces before code leaves your machine.
#   1. Biome over the whole repo (not just staged).
#   2. Full Vitest suite.
# Bypass with `git push --no-verify`.

set -e

echo "▸ bun run lint"
bun run lint

echo "▸ bun run test"
bun run test
